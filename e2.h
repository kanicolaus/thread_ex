
//
// Created by Brian Swenson on 2/8/18.
//
//#pragma once
//#include <thread>

#ifndef E2_E2_H
#define E2_E2_H
// FUNCTION PROTOTYPE
/*
int calcMin(const std::vector<int>& vec) {

}
int calcMax(const std::vector<int>& vec) {
    int t = vec[0];
    for(int i=0; i<vec.capacity(); ++i) {
        t = (t < vec[i]) ? vec[i] : t;
    }
    return t;
}
long calcSum(const std::vector<int>& vec) {
    long t = 0;
    for (auto& n : vec)
    t += n;
    return t;
}
double calcAvg(const std::vector<int>& vec) {
    float t = 0;
    for (auto& n : vec)
    t += n;
    return t/vec.capacity();
}
*/
#endif //E2_E2_H
